meta {
  name: Create shop empty name
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/shops/
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": ""
  }
}

script:post-response {
  test("400 validation", function() {
    expect(res.getStatus()).to.equal(400);
  });
}
