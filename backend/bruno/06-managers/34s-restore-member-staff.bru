meta {
  name: Owner restores member to Staff
  type: http
  seq: 68
}

patch {
  url: {{baseUrl}}/shops/{{shopId}}/managers/{{memberUserId}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "roleId": "{{staffRoleId}}"
  }
}

script:post-response {
  test("200 owner bypasses rank", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
