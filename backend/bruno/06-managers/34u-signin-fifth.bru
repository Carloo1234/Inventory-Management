meta {
  name: Signin fifth switch session
  type: http
  seq: 70
}

post {
  url: {{baseUrl}}/auth/signin
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "{{fifthEmail}}",
    "password": "Password1"
  }
}

script:post-response {
  test("201 jar now fifth", function() {
    expect(res.getStatus()).to.equal(201);
  });
}
