meta {
  name: Signin fourth switch session
  type: http
  seq: 27
}

post {
  url: {{baseUrl}}/auth/signin
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "{{fourthEmail}}",
    "password": "Password1"
  }
}

script:post-response {
  test("201 jar now fourth", function() {
    expect(res.getStatus()).to.equal(201);
  });
}
