meta {
  name: Fourth self-leaves shop
  type: http
  seq: 53
}

delete {
  url: {{baseUrl}}/shops/{{shopId}}/managers/{{fourthUserId}}
  auth: none
}

script:post-response {
  test("200 self-leave needs no permission", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
