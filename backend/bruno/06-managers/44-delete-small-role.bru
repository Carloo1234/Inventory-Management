meta {
  name: Cleanup delete SmallRole
  type: http
  seq: 85
}

delete {
  url: {{baseUrl}}/shops/{{shopId}}/roles/{{smallRoleId}}
  auth: none
}

script:post-response {
  test("200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
