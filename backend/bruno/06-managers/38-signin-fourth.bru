meta {
  name: Signin fourth switch session
  type: http
  seq: 52
}

post {
  url: {{baseUrl}}/auth/signin
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "{{fourthEmail}}",
    "password": "Password1"
  }
}

script:post-response {
  test("201 jar now fourth no manager delete perm", function() {
    expect(res.getStatus()).to.equal(201);
  });
}
