meta {
  name: Signin owner restore session
  type: http
  seq: 82
}

post {
  url: {{baseUrl}}/auth/signin
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "bruno-owner-{{stamp}}@mail.com",
    "password": "Password1"
  }
}

script:post-response {
  test("201 owner again", function() {
    expect(res.getStatus()).to.equal(201);
  });
}
