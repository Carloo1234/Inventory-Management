meta {
  name: Assign other-shop role rejected
  type: http
  seq: 17
}

patch {
  url: {{baseUrl}}/shops/{{shopId}}/managers/{{memberUserId}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "roleId": "{{shopBRoleId}}"
  }
}

script:post-response {
  test("404 role not in this shop", function() {
    expect(res.getStatus()).to.equal(404);
  });
}
