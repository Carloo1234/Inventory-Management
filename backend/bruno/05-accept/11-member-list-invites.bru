meta {
  name: Member lists invites has permission
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/shops/{{shopId}}/invites
  auth: none
}

script:post-response {
  test("200 has invite read", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
