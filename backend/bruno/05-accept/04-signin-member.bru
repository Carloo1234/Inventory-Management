meta {
  name: Signin member switch session
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/auth/signin
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "bruno-member-{{stamp}}@mail.com",
    "password": "Password1"
  }
}

script:post-response {
  test("201 jar now member", function() {
    expect(res.getStatus()).to.equal(201);
  });
}
