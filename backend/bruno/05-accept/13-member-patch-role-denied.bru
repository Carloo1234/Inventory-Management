meta {
  name: Member patch role denied
  type: http
  seq: 14
}

patch {
  url: {{baseUrl}}/shops/{{shopId}}/roles/{{acceptRoleId}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "Hacked"
  }
}

script:post-response {
  test("403 lacks roles update", function() {
    expect(res.getStatus()).to.equal(403);
  });
}
