meta {
  name: Helper delete shop B role
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/shops/{{shopBId}}/roles/{{otherRoleId}}
  auth: none
}

script:post-response {
  test("200 helper cleanup", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
