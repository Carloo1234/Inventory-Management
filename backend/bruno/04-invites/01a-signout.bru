meta {
  name: Signout switch session
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/signout
  auth: none
}

script:post-response {
  test("200 jar cleared so member signin actually switches", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
