meta {
  name: Delete invite
  type: http
  seq: 18
}

delete {
  url: {{baseUrl}}/shops/{{shopId}}/invites/{{inviteId}}
  auth: none
}

script:post-response {
  test("200 revoked", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
