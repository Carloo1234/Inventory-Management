meta {
  name: Signout end of run
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/auth/signout
  auth: none
}

script:post-response {
  test("200 logged out", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
