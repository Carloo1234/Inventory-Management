meta {
  name: Delete InviteViewer role
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/shops/{{shopId}}/roles/{{acceptRoleId}}
  auth: none
}

script:post-response {
  test("200 holder removed earlier", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
