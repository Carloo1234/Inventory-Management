meta {
  name: Delete Cashier role
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/shops/{{shopId}}/roles/{{roleId}}
  auth: none
}

script:post-response {
  test("200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
