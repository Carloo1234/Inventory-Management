meta {
  name: Get role by id
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/shops/{{shopId}}/roles/{{roleId}}
  auth: none
}

script:post-response {
  test("200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
